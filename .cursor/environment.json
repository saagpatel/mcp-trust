{
  "name": "mcp-trust",
  "disableAllMcpServers": true,
  "install": "python3 -m pip install --disable-pip-version-check --user 'uv==0.12.4' && export PATH=\"$(python3 -m site --user-base)/bin:$PATH\" && uv python install 3.11.15 && uv sync --frozen --extra dev && uv run --no-sync ruff check src scripts tests && uv run --no-sync pytest -q"
}
